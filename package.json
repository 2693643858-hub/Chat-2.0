{
  "name": "codex-chat",
  "version": "0.1.0",
  "private": true,
  "description": "A lightweight web chat prototype with a static frontend and Node.js backend.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check server.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=24"
  }
}
