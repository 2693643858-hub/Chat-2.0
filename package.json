{
  "name": "codex-chat",
  "version": "0.1.0",
  "private": true,
  "description": "A lightweight web chat prototype with a static frontend and Node.js backend.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check server.js && node --check public/app.js && node --check scripts/write-netlify-config.mjs",
    "build:netlify": "node scripts/write-netlify-config.mjs"
  },
  "engines": {
    "node": ">=24"
  }
}
